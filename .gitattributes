# Normalize all text files to LF in the repository
* text=auto eol=lf

# Ensure SVGs remain text with consistent endings
*.svg text eol=lf

# Help GitHub linguist highlight Nunjucks templates like HTML
*.njk linguist-language=HTML
